<Page
    x:Class="HotKeySight.Pages.ByAppPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800">

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧：应用列表 -->
        <Border
            Grid.Column="0"
            Background="{ThemeResource CardFillColorDefaultBrush}"
            CornerRadius="8"
            Padding="12"
            Margin="0,0,12,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Text="已注册的应用"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="8,4,0,12" />

                <ListView
                    x:Name="AppListView"
                    Grid.Row="1"
                    SelectionChanged="AppListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <Image
                                    Width="24"
                                    Height="24"
                                    Source="{Binding Icon}"
                                    Margin="0,0,10,0" />
                                <StackPanel>
                                    <TextBlock
                                        Text="{Binding Name}"
                                        FontSize="14"
                                        FontWeight="Medium" />
                                    <TextBlock
                                        Text="{Binding HotkeyCount}"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- 右侧：选中应用的热键列表 -->
        <Border
            Grid.Column="1"
            Background="{ThemeResource CardFillColorDefaultBrush}"
            CornerRadius="8"
            Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    x:Name="AppNameText"
                    Text="选择要查看的应用"
                    FontSize="20"
                    FontWeight="Bold"
                    Margin="0,0,0,4" />

                <TextBlock
                    Grid.Row="1"
                    x:Name="AppPathText"
                    Text=""
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Margin="0,0,0,16" />

                <ListView
                    x:Name="HotkeyListView"
                    Grid.Row="2">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{ThemeResource CardFillColorSecondaryBrush}"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock
                                        Grid.Column="0"
                                        Text="{Binding Description}"
                                        FontSize="14"
                                        VerticalAlignment="Center" />

                                    <Border
                                        Grid.Column="1"
                                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                                        CornerRadius="4"
                                        Padding="8,4">
                                        <TextBlock
                                            Text="{Binding HotkeyText}"
                                            FontSize="13"
                                            FontWeight="SemiBold"
                                            Foreground="White" />
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- 空状态 -->
                <StackPanel
                    x:Name="EmptyState"
                    Grid.Row="2"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Visibility="Collapsed">
                    <SymbolIcon Symbol="AllApps" FontSize="48" />
                    <TextBlock
                        Text="从左侧选择一个应用"
                        FontSize="16"
                        HorizontalAlignment="Center"
                        Margin="0,12,0,0" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
