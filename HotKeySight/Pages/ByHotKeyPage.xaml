<Page
    x:Class="HotKeySight.Pages.ByHotKeyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部说明 -->
        <Border
            Grid.Row="0"
            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
            CornerRadius="8"
            Padding="20"
            Margin="0,0,0,16">
            <StackPanel>
                <TextBlock
                    Text="热键检测"
                    FontSize="20"
                    FontWeight="Bold"
                    Margin="0,0,0,8" />
                <TextBlock
                    Text="按下任意组合键，查看是哪个应用注册了该热键"
                    FontSize="14"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                <!-- 按下的热键显示 -->
                <Border
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="24,16"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Center">
                    <TextBlock
                        x:Name="PressedKeyText"
                        Text="等待按下热键..."
                        FontSize="28"
                        FontWeight="Bold"
                        Foreground="White"
                        HorizontalAlignment="Center" />
                </Border>
            </StackPanel>
        </Border>

        <!-- 结果列表 -->
        <Border
            Grid.Row="1"
            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
            CornerRadius="8"
            Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Text="注册该热键的应用"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Margin="0,0,0,12" />

                <ListView
                    x:Name="ResultListView"
                    Grid.Row="1">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                                CornerRadius="6"
                                Padding="12,10"
                                Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Image
                                        Grid.Column="0"
                                        Width="32"
                                        Height="32"
                                        Source="{Binding Icon}"
                                        Margin="0,0,12,0" />

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock
                                            Text="{Binding AppName}"
                                            FontSize="14"
                                            FontWeight="Medium" />
                                        <TextBlock
                                            Text="{Binding Description}"
                                            FontSize="12"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>

                                    <Border
                                        Grid.Column="2"
                                        Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
                                        CornerRadius="4"
                                        Padding="8,4"
                                        Visibility="{Binding IsConflict, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock
                                            Text="冲突"
                                            FontSize="12"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- 空状态 -->
                <StackPanel
                    x:Name="EmptyState"
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                    <TextBlock
                        Text="⌨️"
                        FontSize="48"
                        HorizontalAlignment="Center" />
                    <TextBlock
                        Text="按下热键后结果会显示在这里"
                        FontSize="14"
                        HorizontalAlignment="Center"
                        Margin="0,12,0,0"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
